{* TO DO: a proper title *}
{capture assign="page_title"}photos from your contacts | that have been poked recently{/capture}
{include file="inc_head.txt"}

<h2>Your contacts photos (that have been poked recently)</h2>

<p style="font-size:small;"><em>Poked?</em> These are photos from your contacts that have been uploaded, commented on or maybe faved sometime in the last 12 hours. That sort of thing. The photos
are grouped by photographer, sorted by the photographer with the photo that's
been most recently... <em>poked</em>.</p>

{if $new_subscription}

{if $subscription_ok}
<p>Okay! Your subscription to new photos from your contacts has been
registered. You should start seeing photos shortly.</p>
{else}
<p>Ack! Something when wrong trying to subscribe to your contacts photos.</p>
{/if}

{else}

<p style="font-size:small;">Here they are:</p>

{assign var="contacts" value=$users_updated|@array_keys|@count}
{assign var="columns" value="3"}
{assign var="per_column" value=$contacts/$columns|@ceil}
{assign var="i" value="1"}

{foreach from=$users_updated item="ts" key="nsid" name="contacts"}

{if $i==1}<ul class="ffbp_list">{/if}

<li><a href="#{$nsid|escape}">{$users_names.$nsid|escape}</a></li>

{if $i==$per_column or $smarty.foreach.contacts.last}
{assign var="i" value=1}
</ul>
{else}
{assign var="i" value=$i+1}
{/if}

{if $smarty.foreach.contacts.last}</div><br clear="all" />{/if}

{/foreach}


</ul>

{foreach from=$users_updated item="ts" key="nsid"}
<a name="{$nsid|escape}"></a>
<h3>{$users_names.$nsid|escape}</h3>

<div class="ffbp_buddyicon">
<img src="{$cfg.abs_root_url}buddyicon/{$nsid|escape}.jpg" height="48" width="48" />
</div>

<div class="ffbp_photos">
{foreach from=$users_photos.$nsid item="row"}
<div class="ffbp_photo">
<a href="http://www.flickr.com/photos/{$row.owner|escape}/{$row.photo_id|escape}/" target="_flickr" title="{$row.title|escape}"><img src="{$row.thumb_url}" alt="{$row.title|escape}, by {$row.ownername|escape}" /></a>
</div>
{/foreach}
</div>

<br clear="all" />

{/foreach}

{/if}

{include file="inc_foot.txt"}
